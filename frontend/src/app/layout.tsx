import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MentorSpace — 1-on-1 Mentorship Platform",
  description: "Connect with expert mentors in real-time. Code together in a VS Code-grade editor, video call via WebRTC, and chat live — all in one private workspace.",
  keywords: ["mentorship", "1-on-1", "coding", "video call", "real-time", "WebRTC", "Monaco editor"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
